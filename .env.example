# ---------------------------------------------------------------------------
# MasterMind: Story Builder — Environment Variables
# ---------------------------------------------------------------------------
# 1. Copy this file to a new file named `.env` (in the same folder).
# 2. Fill in the two values below from your Supabase dashboard.
# 3. Save. Never commit `.env` to git — `.gitignore` already blocks it.
# ---------------------------------------------------------------------------

# Your Supabase project's base URL.
# Example: https://abcdefghij.supabase.co
# Where to find it: it's the first part of any URL Supabase gives you,
# e.g. the Data API URL minus the trailing /rest/v1/.
VITE_SUPABASE_URL=

# Your Supabase "publishable" API key (also shown as "anon public" in the
# legacy UI). This one is safe to ship in frontend code.
# Where to find it: Supabase dashboard → Project Settings → API Keys → "Publishable key"
# DO NOT use the "secret" or "service_role" key here.
VITE_SUPABASE_ANON_KEY=

# PostHog project token (also shown as "Project API Key" in their docs).
# Starts with `phc_...`. Safe to ship in frontend code — it's a write-only
# ingest key, not a server-side secret.
# Where to find it: PostHog dashboard → Project Settings → Project API Key.
# Only used for users flagged is_test_user=true in public.profiles
# (see ADR-0009). Leaving this blank disables PostHog entirely.
VITE_POSTHOG_KEY=

# NOTE (2026-07-28): VITE_POSTHOG_HOST is no longer used. Analytics traffic
# now goes through a first-party reverse proxy at /relay (so ad-blockers
# don't swallow tester sessions). The PostHog region (US) is configured in
# vercel.json (production) and vite.config.js (local dev) — change it there,
# in both files, if the PostHog project ever moves regions.
